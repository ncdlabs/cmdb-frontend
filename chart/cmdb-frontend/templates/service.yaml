apiVersion: v1
kind: Service
metadata:
  name: {{ include "cmdb-frontend.fullname" . }}
  labels:
    {{- include "cmdb-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "cmdb-frontend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
